# Claims Register

Every material public claim must have an owner and evidence before release.

**Evidence basis:** Rows below were verified 2026-08-24 against the application repository (`C:\Users\Tonym\widian`) — schema (`scripts/sql/`), enforcement logic and `context/current-state.md` — under explicit one-time authorisation to inspect that repository for this purpose. This is repository/code evidence, not confirmed live staging behaviour; re-verify against deployed staging before publication per the source-precedence order in `WIDIAN_MARKETING_WEBSITE_CONTEXT.md` §16.

| Claim area | Current public treatment | Evidence source | Owner | Status |
|---|---|---|---|---|
| One structured building record | Describe only the connected data model (organisation → building → element/asset → inspection → finding → remedial work, all FK-linked). Do not claim a single unified "Building Record" view/API exists yet — that is a documented future-phase target | `scripts/sql/0000_base_schema.sql` (relational chain, lines 446–671, 894–960); `context/current-state.md` confirms unified record UI/API not yet built | Product | Qualified |
| Multi-organisation roles and permissions | Describe only implemented roles: `super_admin`, `widian_admin`, `surveyor`, `supplier` (contractor), `client`, `client_admin`. Enforced via row-level security, not just UI labels | RLS policies scoped via `get_user_org_id()` (`0000_base_schema.sql` lines 964–1078); live-verified cross-tenant 403 denial, 2026-08-12 (`context/current-state.md`) | Engineering/Product | Verified |
| Inspection-to-closure lifecycle | Per D-015: the 10-stage chain (`Recorded → Submitted → Reviewed → Finding raised → Assigned → In progress → Remediated → Independently checked → Client verified → Closed`) is on the product build plan, targeted to ship before website launch. Copy may be written to this target lifecycle, **labelled/treated as a "Proposed future state" claim, not current functionality**, until implementation ships. Today's actual states remain: inspection `overall_status` plus a QA cycle (`awaiting_review → in_review → approved/returned/escalated`); work orders `open → in_progress → completed_reinspect → verified_closed` (P3–P5) or → `awaiting_client_verification → verified_closed` with `client_rejected` rework (P1/P2 only). Mandatory pre-launch gate: re-verify against deployed product before this copy is presented as describing current behaviour; if the build slips past launch, the copy must carry a visible future/in-progress label per `AGENTS.md` invariant 11 | `scripts/sql/0012_inspection_qa_workflow.sql`, `0018_client_verification.sql`; `context/current-state.md` lines 103–113; target state per D-015 (2026-08-24 product-team direction) | Product/Copy | Proposed future state — pre-launch re-verification required |
| Independent inspection | Today the product enforces that the original submitter cannot QA-review their own inspection (self-review forbidden), restricted to permissioned reviewers; there is **not** yet a distinct "independent inspector" role. Whether a dedicated independent-inspector persona ships as part of the D-015 lifecycle build is unconfirmed — do not presume it without separate evidence. Do not present "independent inspector" as an existing persona until re-verified | `transition_inspection_qa()` self-review block (`0012_inspection_qa_workflow.sql`); deferral confirmed at `context/current-state.md` line 112 | Product/SME | Qualified |
| Client verification and closure | Today client verification is enforced **only for P1/P2 priority findings**. Whether the D-015 lifecycle build extends client verification to all findings is unconfirmed. Until re-verified, copy must state the current P1/P2-only scope explicitly, not imply universal client verification | `work_order_verifications` table, `assertWorkOrderVerificationAccess`; live-verified on staging, 2026-08-19 (`context/current-state.md` line 111) | Product/Legal | Qualified |
| Time-stamped attribution and audit history | Claim only for implemented events: immutable `access_audit_events` and `work_order_transitions` tables, `inspection_qa_reviews`, and actor + timestamp columns on core tables | `scripts/sql/0000_base_schema.sql` (audit tables, lines 251–260, 820); `0026_work_order_transitions` | Engineering | Verified |
| Evidence hashing | Explain scope and limitation; never equate with universal legal immutability. SHA-256 hashing (Web Crypto) is implemented for inspections, evidence photos, signatures, work orders and issued reports, used for content-addressed storage and integrity checking — not external notarisation or chain-of-custody certification | `lib/storage/evidence.ts` (`sha256HexServer()`); hash-format CHECK constraints in schema; `lib/reports/verify-report.ts` | Engineering/Security | Qualified |
| Immutable issued records | Verified, narrowly scoped to the `issued_reports` table: a database trigger blocks deletion and blocks any update except a one-way `issued → superseded/withdrawn` status change; original bytes, hash and template version are frozen. Scope any public claim to "issued reports", not "all records" | `enforce_issued_report_immutability` trigger (`0020_issued_reports.sql`); confirmed live at `context/current-state.md` line 124 | Engineering/Security | Verified — scope to issued reports only |
| Security, hosting and resilience | Use honest placeholders until approved facts exist. `SECURITY.md` in the application repo is an unfilled template with no real content. RLS enforcement and Vercel/Supabase managed infrastructure are real, but no formal security-practice write-up exists yet to publish | `context/architecture.md` (stack); `SECURITY.md` (placeholder); `context/current-state.md` line 130 (20 tables / 6 storage buckets RLS-verified) | Security/Legal | Withhold — good candidate to resolve once a security write-up exists |
| Certifications/accreditations | Never imply or display without current proof. Confirmed not currently held. The application repository's own internal rule already states not to claim ISO 27001, CREST or Cyber Essentials before independently achieved and current | `context/product-strategy.md` lines 185, 301, 336–353; `context/specs/16-…blueprint.md` lines 1010–1069 (roadmap only) | Legal/Security | Withhold |
| Customer outcomes | No invented metrics or testimonials | Not assessed by code inspection — requires permissioned case-study evidence | Marketing/Customer | Withhold |
| Compliance marketplace infrastructure | Strategic direction only if marketplace behaviour is not live | Not assessed by code inspection — strategic/roadmap claim | Founder/Product | Proposed future state |
| Offline inspection capture and sync | Core loop only: inspection and finding capture, local queue (Dexie/IndexedDB), and sync with per-record retry and idempotent writes are implemented and live-verified. Scheduling/agenda offline prefetch and full history sync are **not** offline — copy must name the specific actions that work offline, not claim blanket offline capability | `lib/db/dexie.ts`; live-verified 2026-08-11, gaps noted at `context/current-state.md` lines 49–60, 80 | Engineering/Product | Qualified |
| Guest contractor completion | Contractors can complete work via a guest link without a full account: SHA-256-hashed, expiring, rate-limited access tokens | `work_orders.guest_access_token_hash`; `/api/guest/work-order/[token]`; `context/current-state.md` line 109 | Product | Verified |
| QR scanning, photo evidence and P1–P5 priorities | QR-based asset lookup, photographic evidence capture and P1–P5 defect priority are implemented and used throughout the inspection workflow | `components/inspection/qr-scanner.tsx`, `components/inspection/asset-search.tsx`; `defect_priority` enum in schema | Product | Verified |
| Issued doorset records | Implemented and immutable (see issued-records row above for exact scope). Public/QR verification of an issued record by a third party is planned, not built — label as future if referenced | `lib/reports/doorset/issue-report.ts`; `components/reports/doorset/AssetHeader.tsx`; verification-portal gap noted at `context/current-state.md` line 131 | Product | Verified; public verification portal is Proposed future state |
| Evidence-pack / compliance-pack generation | Building/compliance evidence packs are generated as a ZIP (PDF + supporting files) including inspection history | `lib/compliance/generate-compliance-pack.ts`; confirmed at `context/current-state.md` line 122 | Product | Verified |
| Fire Risk Assessment capability | Do not describe a dedicated FRA commissioning/scope/hazards-and-controls/significant-findings/QA workflow — it does not exist. What's real: an expiry-date/status field on each building (`fra_expiry_date`, `overdue_fra`, the "FRA clock") used for portfolio attention-ranking, plus general "FRA-style checklist paths" available inside the same inspection UI used for fire-door/fire-stopping work (not a distinct FRA product surface). Any FRA marketing copy must describe only these two things, or be withheld entirely, until Product Truth confirms a fuller scope | `lib/portfolio/building-status.ts` (`isFRAExpired`/`rankBuildingsNeedingAttention`); `context/current-state.md` line 85 ("FRA-style checklist paths exist in the current inspection UI"); `context/specs/16-product-gtm-implementation-blueprint.md` U16-PH0C-01; no FRA-specific schema found in `scripts/sql/` beyond the two building columns above | Product | Qualified — narrowly scoped, see `DECISIONS.md` D-026 |

## Claim states

- **Verified:** evidence reviewed, wording and scope approved.
- **Qualified:** true only with an explicit limitation beside the claim.
- **Proposed future state:** visibly labelled as planned or being developed.
- **Withhold:** not suitable for public use.

## Release rule

No copy moves from draft to production while its supporting claim is “Verify” or “Withhold”. Screenshots and interface demonstrations are claims too; test data and future controls must be labelled.
