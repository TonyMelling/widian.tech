# Decision Log

Record decisions when made; do not silently convert assumptions into requirements.

| ID | Decision | Status | Rationale / next evidence |
|---|---|---|---|
| D-001 | Marketing site is a separate repository and deployment at `widian.tech` | Accepted | User direction |
| D-002 | Staging uses `staging.widian.tech` and is linked from the agreed environment/filter control | Accepted, wording to clarify | Confirm exact location and audience of “filter” |
| D-003 | Product application uses `app.widian.tech` | Accepted | Domain split approved in principle; DNS, Clerk authentication migration and rollback runbook remain required follow-up work before DNS cutover |
| D-004 | Creative platform is “One Record, Unbroken” | Accepted | User direction |
| D-005 | First release uses routed pages, not homepage anchors | Accepted | User direction |
| D-006 | Approved shield and wordmark must be used | Accepted | Interim PNGs staged at `public/brand/` (`widian-logo.png`, `widian-mark-transparent.png`), sourced with explicit authorisation from the application repository. SVG exports and a reversed lock-up variant remain outstanding. |
| D-007 | Canonical lead store is an isolated marketing database, separate from the operational Widian product database | Accepted | User direction; requires a basic lead console/operating process before launch since no CRM pipeline exists |
| D-008 | Transactional email provider is Resend, on a dedicated sending subdomain | Accepted | User direction; SPF/DKIM/DMARC configuration for the sending subdomain remains an implementation task |
| D-009 | Walkthrough booking is immediate calendar selection (no human qualification gate before booking) | Accepted | User direction; form should still capture qualification fields (organisation, role, portfolio size) even though booking is immediate, so the walkthrough owner arrives prepared |
| D-010 | Retention and privacy basis for enquiries/marketing | Open — blocked | No privacy owner assigned yet (2026-08-24). This is a hard pre-launch gate per `PRIVACY_AND_UK_GDPR_PLAN.md`; cannot be resolved without a named owner's approval |
| D-011 | Analytics approach is Google Analytics 4 with Consent Mode | Accepted | User direction; requires a full cookie-consent banner with equally prominent accept/reject choices before GA4 loads (`PRIVACY_AND_UK_GDPR_PLAN.md` §Cookies and analytics), and Google's data-processing terms need review — remains blocked on the privacy owner assignment (D-010/D-012) for final sign-off |
| D-012 | Lawful bases and retention schedule by processing purpose | Open — blocked | No privacy owner assigned yet (2026-08-24); same blocker as D-010 |
| D-013 | SEO topic priorities and canonical URL set | Open | User will complete keyword/SERP research separately (outside this session); release-one sitemap in `WIDIAN_MARKETING_WEBSITE_SPEC.md` §2 stands as the working URL set in the meantime |

| D-014 | Claims register populated from application-repo code inspection, one-time explicit authorisation | Accepted | Evidence recorded 2026-08-24 in `CLAIMS_REGISTER.md`; still requires deployed-staging re-verification before publication per source-precedence order |
| D-015 | Marketing lifecycle language (10-stage chain) does not yet match implemented product states, but the 10-stage chain is on the product build plan and is targeted to be implemented before website launch | Accepted, contingent | Marketing copy may be written to the target 10-stage lifecycle. This is a "Proposed future state" claim, not "Verified", until the states actually ship. It must be re-verified against deployed product as a pre-launch gate before the copy is treated as describing current functionality — see `CLAIMS_REGISTER.md` "Inspection-to-closure lifecycle" row and the new pre-launch re-verification item in `PROGRESS_TRACKER.md`. If the build slips past website launch, the lifecycle copy must be labelled as future/in-progress per `AGENTS.md` invariant 11, not presented as current. Additional gap found 2026-08-24: the documentation itself is internally inconsistent about the stage count — `WIDIAN_MARKETING_WEBSITE_SPEC.md` §7.3 calls it a "seven-stage responsibility gate", `WIDIAN_MARKETING_WEBSITE_CONTEXT.md` §5's target lifecycle has eight stages, its "Required status distinctions" list has ten, and `WIDIAN_MARKETING_WEBSITE_SPEC.md` §4's "Mechanism" line has six. Pick one canonical stage model before How It Works' interactive gate is built — see `docs/copy/how-it-works.md`. **Resolved 2026-08-24: see D-023** — the 10-stage chain is now the canonical model, still contingent on the build shipping and being re-verified. |

| D-016 | Primary CTA label is "Book a Widian walkthrough" on every page, unvaried | **Superseded by D-025** (2026-08-24) — label changed to "Book a Demo" | Resolves a contradiction found 2026-08-24: `SPEC.md` §19 acceptance criteria requires the primary CTA to be "consistently 'Book a Widian walkthrough'", while `CONTENT_AND_COPY_PLAN.md`'s page-brief table assigns different primary-CTA text per page. The explicit, testable acceptance criterion wins. Page differentiation is carried by each page's lead-in argument and secondary CTA instead, not by varying the primary button label. `docs/copy/how-it-works.md` and `docs/copy/platform.md` corrected to match; `docs/copy/home.md` was already consistent. The *principle* (one unvaried primary label sitewide) survives D-025's rewording — only the label text changed. |

| D-017 | Site-wide "launching soon" ticker banner added above the header, per user direction (2026-08-24) | Accepted | Wording deliberately generic ("Widian is launching soon — book a walkthrough to be among the first") since exact launch status (available product / pilot / early-access programme) is still an open input per `SPEC.md` §20. Revisit wording once that's confirmed. Implemented with a `prefers-reduced-motion` static fallback; Ember used only on the small label chip to stay scarce per the brand invariant. See Home wireframe artifact. |

| D-018 | Styling approach is Tailwind CSS 4 (CSS-based `@theme` tokens), not hand-written token CSS | Accepted | Both were permitted by `ARCHITECTURE.md` §3. Tailwind's v4 `@theme` block maps directly onto the documented design tokens (Navy, Ember, spacing/radius scale) without inventing a parallel system, and speeds implementation for the vertical slice. Reason logged per `AGENTS.md` rule 10 (dependency additions must be documented) |
| D-019 | Test tooling is Vitest 4 + Testing Library + jsdom for component tests | Accepted | Matches `ARCHITECTURE.md` §3's recommendation ("Vitest or equivalent"). Added 2026-08-24 alongside the Home vertical slice so the "unit/component tests" gate in `AGENTS.md` is genuinely run, not skipped. Playwright (browser journeys) not yet added — manual Playwright-driven checks were run instead for this slice; formal Playwright test files are a follow-up |
| D-020 | Header/footer nav breakpoint set at `lg` (1024px), not `md` (768px) | Accepted | Found during browser testing 2026-08-24: at 768px the full desktop nav (5 links + Sign in + CTA) doesn't fit and wraps onto the launch ticker. Tablet widths now get the mobile hamburger nav instead |

| D-021 | Product demonstrations may use synthetic/illustrative data, clearly labelled as such, rather than waiting for real product screenshots | Accepted | User direction, 2026-08-24 redesign brief. Unblocks interactive elements (Building Record drill-down, lifecycle stepper, evidence comparison) that were stalled on real screenshot approval. Every such demonstration must be visibly labelled illustrative/synthetic, never presented as a real customer record, per `AGENTS.md` invariant 11 and `CONTENT_ASSET_INVENTORY.md`'s asset rules |
| D-022 | Building Record is a separate page (`/building-record`) from Platform (`/platform`) | Accepted | User direction, 2026-08-24. Platform explains what each of the nine record layers records and why it persists (static anatomy); Building Record explains how a user navigates the structure (portfolio → building → element → inspection → finding drill-down). Added to `NAV_LINKS` and route tree; not yet in the sitemap (still a stub) or `CLAIMS_REGISTER.md`, which are populated once the page is actually built. Note: a repository check the same day found the drill-down levels proposed in the redesign brief ("Location", "Case") do not match the verified schema — see `docs/copy/building-record.md` §4 |
| D-023 | Canonical lifecycle stage model is the 10-stage chain (`Recorded → Submitted → Reviewed → Finding raised → Assigned → In progress → Remediated → Independently checked → Client verified → Closed`), resolving D-015's stage-count contradiction | Accepted, contingent | User direction, 2026-08-24. This does not change D-015's underlying gate: the 10-stage chain remains a "Proposed future state" claim per `CLAIMS_REGISTER.md`, not current functionality, until the product build actually ships and is re-verified against deployed staging. Marketing copy for How It Works, Building Record and any Verified Closure content may now be written to this specific 10-stage model instead of leaving the stage count open, but must carry a visible future/in-progress label per `AGENTS.md` invariant 11 until the build ships. If the build slips past website launch, this copy must not be presented as describing current behaviour |
| D-024 | Verified Closure is a section within `/how-it-works`, not a separate page | Accepted | User direction, 2026-08-24. Avoids splitting one lifecycle story (which already ends in verified closure) across two pages with overlapping narratives |
| D-025 | Primary CTA label changed from "Book a Widian walkthrough" to "Book a Demo" sitewide; route renamed `/book-a-walkthrough` → `/book-a-demo` | Accepted, supersedes D-016 | User direction, 2026-08-24 redesign brief. Updated `Button.tsx`, `SiteHeader.tsx`, `MobileNav.tsx`, `SiteFooter.tsx`, `app/page.tsx`, `app/platform/page.tsx`, associated tests, and `docs/copy/book-a-demo.md` (renamed from `book-a-walkthrough.md`). Internal/non-visible naming (the `POST /api/walkthrough` endpoint sketched in `ARCHITECTURE.md`, the lead lifecycle's "Walkthrough booked" status in `LEAD_CAPTURE_AND_COMMUNICATIONS.md`, the `walkthrough_*` analytics event names in `WIDIAN_MARKETING_WEBSITE_SPEC.md` §15) is deliberately left unchanged — those are internal/engineering identifiers, not visitor-facing copy, and renaming them has no user-facing benefit against a real rework cost. Revisit only if that inconsistency itself becomes confusing during implementation |
| D-026 | Fire Risk Assessment marketing content is blocked pending Product Truth review | Open — blocked | Repository check performed 2026-08-24 (same one-time authorisation basis as `CLAIMS_REGISTER.md`) found FRA is currently only an expiry-date/status field on a building (`fra_expiry_date`, `overdue_fra`) used for portfolio attention-ranking (the "FRA clock", `lib/portfolio/building-status.ts`), plus generic "FRA-style checklist paths" inside the same inspection UI used for fire-door/fire-stopping work — there is no dedicated FRA commissioning, scope-and-preparation, hazards/controls, significant-findings or FRA-specific QA workflow distinct from the general inspection/finding/work-order model already in `CLAIMS_REGISTER.md`. A "Fire Risk Assessments" page written to the full brief (commissioning, scope, field collection, hazards/controls, significant findings, action plans, QA, issue/review) would overclaim significantly. Do not draft this page's copy until Product Truth confirms current vs planned FRA scope explicitly | User instruction 2026-08-24: "check repo (new function)" |
| D-027 | Replace the looping launch ticker with a static launch notice and Demo link | Proposed — pending Motion Designer + Accessibility Reviewer | The automatic 20-second marquee required a user pause mechanism and competed with the quiet-authority principle. Static treatment preserves the announcement, removes continuous motion and needs no reduced-motion substitute. Implemented 2026-08-24; specialist review still required. |
| D-028 | Use generated editorial imagery only as visibly labelled conceptual illustration; use code-native synthetic views for record demonstrations | Proposed — pending Creative Director + Product Truth Reviewer | Three optimised WebP assets were added for the building cutaway, surveyor and contractor stories. They contain no customer data or readable product UI. Synthetic record/status values are labelled in the component and must never be presented as product screenshots or current customer evidence. |
| D-029 | Platform hero uses a code-native animated record-assembly model rather than an animated logo | Proposed — pending Creative Director + Motion Designer + Accessibility Reviewer | Nine labelled record layers assemble around one Building Record anchor using CSS only, keeping the page server-rendered and avoiding a new animation dependency or client bundle. The visual is explicitly labelled conceptual, does not reproduce unverified product UI, and resolves to a complete static diagram under reduced motion. The approved logo remains stable. |
| D-030 | Building Record’s dominant upper diagram is the approved ten-step target lifecycle for one synthetic finding | Proposed — pending Product Truth + Creative Director + Accessibility Reviewer | Revised following user review: the previous six-level schema drill-down made the primary diagram appear to stop at six steps. The upper interaction now shows all ten responsibility stages while retaining the verified building/element/finding hierarchy as context. It remains visibly labelled synthetic and proposed pending pre-launch verification. |
| D-031 | Home shows the launch announcement as a modal once per browser-tab session; all other visits use the static ticker | Proposed — pending Creative Director + Accessibility Reviewer | The modal is limited to the first Home appearance and records that appearance immediately in `sessionStorage`. Returning Home in the same tab shows the ticker, preventing repeated interruption. A native dialog provides focus trapping, Escape dismissal and a reduced-motion-safe presentation. |
| D-032 | Rename the public “The New Market” label to “Market Direction” and present its argument as an interactive status-aware roadmap | Proposed — pending Brand Strategist + Creative Director + Product Truth Reviewer | “Market Direction” signals future intent without implying an established marketplace. The roadmap distinguishes current conditions, operating principles, a verified foundation, expected value, strategic hypothesis and future possibility. The route remains `/new-market` to avoid unnecessary URL churn while the page is `noindex`. |
| D-033 | Apply restrained CSS scroll-linked parallax only to contained editorial imagery | Proposed — pending Motion Designer + Accessibility Reviewer | The effect reinforces architectural depth on Home and the Solutions accordion without moving copy or controls. It progressively enhances in browsers supporting view timelines and resolves to a static image under reduced motion. No scroll listener or animation dependency is added. |

| D-034 | Widian is the data controller for marketing-site and demo-enquiry processing; lead data uses a separate Supabase marketing database and the site uses Vercel configured for an EU AWS region | Accepted direction — operational and privacy verification pending | User direction, 2026-08-24. Exact legal identity, contacts, service regions, processor terms, subprocessors and transfer safeguards remain pre-publication evidence requirements. This does not authorise an unqualified claim of GDPR/DPA compliance. |
| D-035 | Adopt an internal information-security policy template before broader public security-practice claims | Proposed — owner and executive approval required | `docs/SECURITY_POLICY_TEMPLATE.md` defines the control and evidence structure. It is not evidence that every control operates and must not be cited publicly until owners approve it and implementation evidence exists. |

| D-036 | Market Direction opens with a generated editorial pressure field rather than a product or marketplace diagram | Proposed — pending Brand Strategist + Creative Director + Product Truth + Accessibility review | The visual creates controlled operational tension through fragmented evidence, hand-offs and responsibility markers converging around one persistent building. One scarce Ember thread suggests the Building Record resolution. It includes no fire/disaster imagery, fabricated regulation, dates, customer data, participant scale or live product UI and is visibly labelled conceptual. |

| D-037 | Deploy the marketing site through Git-connected Vercel previews before attaching `widian.tech` | Accepted | Vercel matches the Next.js runtime, supplies isolated preview URLs and supports rollback and the planned server-side demo endpoint. Preview and local robots disallow crawling; production crawling remains controlled per route. FTP/static export is not selected because it would remove runtime support and require separate image/form infrastructure. |

| D-027 | Resources section (`/resources`) un-deferred and built as file-based Markdown, no CMS | Accepted | User is actively writing articles (2026-08-24), removing the "no content/no owner" reason it was deferred in `WIDIAN_MARKETING_WEBSITE_SPEC.md` §2. Built per `ARCHITECTURE.md` §6's guidance (local content, no CMS until an editorial need justifies one): each article is a Markdown file with frontmatter in `content/resources/`, parsed with `gray-matter` + `marked` (new dependencies, justified per `AGENTS.md` rule 10 — both are small, single-purpose, widely-used libraries with no runtime services). Index and sitemap only include non-draft articles; empty state shown until the first real article lands. Article JSON-LD (`Article` schema) added per `SEO_STRATEGY_AND_TECHNICAL_PLAN.md`, now genuinely applicable |

| D-028 | Ember-as-text-colour and the amber "Qualified" badge changed to pass WCAG AA contrast | Accepted | Following a review against the WebAIM Million accessibility report (2026-08-24): ember `#E8431A` as a *text* colour on white/light backgrounds (nav link hover, secondary button/link hover, resource article links) measured 4.00:1, failing AA's 4.5:1 minimum for normal text. Changed those specific instances to `ember-hover` (`#C93A17`, already an existing token) which passes at 5.12:1. Ember is unchanged everywhere it's a background, icon or priority marker — that usage already passed. The "Qualified" claim badge (`RecordSpine.tsx`) changed from amber-outline text (3.59:1 on navy, failing) to filled amber background + white text (4.71:1, passes — corrected from an initially miscalculated 5.32:1, caught during implementation), matching the existing "Verified" badge treatment. **Not yet fixed, flagged for a separate decision**: the same failing ember-on-white pairing also appears in the sitewide "eyebrow label" pattern (`text-ember uppercase`, used on nearly every page) — this is a much larger visual-identity-level change than the link/badge fixes and wasn't included without explicit sign-off; see `DESIGN_SYSTEM.md` §11 |

## Decision entry template

- ID and date
- Owner and approvers
- Context and options considered
- Decision and rationale
- Consequences and follow-up actions
- Evidence or links
- Review trigger/date
